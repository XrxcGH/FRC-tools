# Measured numbers

_Generated by `npm run measure`. Every figure below was produced by running the code, not by estimating. The final section lists what this harness **cannot** measure — those stay open rather than being filled with plausible values._

Node v24.16.0 · win32/x64

## 1. Envelope size vs body size

| Body bytes | Record payload | Envelope | Overhead |
|---:|---:|---:|---:|
| 0 | 101 | 186 | 186 |
| 16 | 117 | 202 | 186 |
| 40 | 143 | 228 | 188 |
| 80 | 183 | 268 | 188 |
| 120 | 223 | 308 | 188 |
| 256 | 361 | 447 | 191 |
| 1024 | 1129 | 1215 | 191 |

Overhead is 186-191 bytes and steps only where CBOR length headers widen (186 below a 24-byte body, 188 below 256, 191 beyond). It is 101 bytes of record fields — dominated by two 32-byte hashes — plus 21 bytes of COSE structure and a 64-byte Ed25519 signature. That is the price of every record being independently verifiable, which is what lets a device accept a record relayed by a peer it has no reason to trust.

## 2. A full event day

| Quantity | Value |
|---|---:|
| Records (80 quals x 8 scouts) | 640 |
| Distinct observations | 640 |
| Distinct scouts | 8 |
| Total envelope bytes | 167.5 kB |
| Mean envelope | 268 B |
| Seal + verify + store, wall clock | 1567 ms |
| Per record | 2.45 ms |

Transfer time for the whole day's data, at the throughput figures the design uses:

| Path | Assumed rate | Time for full day |
|---|---:|---:|
| BLE GATT, iOS peripheral (design figure) | 12 kB/s | 14.0 s |
| BLE GATT, Android central (design figure) | 25 kB/s | 6.7 s |
| USB mass storage | 5000 kB/s | 33 ms |

Bandwidth is not the constraint. A full competition day of scouting moves in well under a minute even on the slowest assumed path — which is the measured basis for cutting L2CAP from v1 (D-13). Connection setup and radio duty cycle are what matter.

## 3. Reconciliation cost vs symmetric difference

Two stores, 640 records each at full overlap, then diverged by the stated amount (half missing from each side). "Overhead" excludes the transferred records themselves — it is what the protocol costs to *find* the difference.

| Symmetric difference | Messages | Round trips | Total bytes | Protocol overhead | Converged |
|---:|---:|---:|---:|---:|---|
| 0 | 1 | 1 | 0.0 kB | 0.0 kB | yes |
| 2 | 4 | 2 | 6.2 kB | 5.7 kB | yes |
| 10 | 4 | 2 | 8.2 kB | 5.6 kB | yes |
| 50 | 4 | 2 | 18.7 kB | 5.7 kB | yes |
| 100 | 6 | 3 | 31.7 kB | 5.5 kB | yes |
| 200 | 10 | 5 | 58.4 kB | 6.0 kB | yes |
| 400 | 16 | 8 | 111.7 kB | 7.0 kB | yes |

Two separate effects are visible here, and they should not be confused.

**Discovery is flat.** Finding *which* records differ always costs the same two round trips and ~5.5-7 kB, whether the difference is 2 records or 400. That is the property that matters on a transport where each round trip costs ~1.8 s of connection setup. The overhead is dominated by a single truncated-id list at 8 bytes per record *held* — so it scales with store size, not difference size, which is the deliberate trade `LEAF_THRESHOLD` (2048) makes: it buys a round trip for a few kB.

**Transfer is chunked**, at 32 records per frame, which is why round trips grow past a ~50-record difference. This is deliberate. A message is atomic, so an unchunked transfer is all-or-nothing: a link dying at 7 s of an 8 s transfer would deliver nothing, silently falsifying the claim that a half-finished session is simply a smaller one. Each frame that lands is progress that survives the drop, and the extra frames cost bandwidth rather than connection setup, since they ride an already-open link.

## 4. Crypto throughput

| Operation | Per record | Records/second |
|---|---:|---:|
| Seal (encode + Ed25519 sign) | 0.47 ms | 2114 |
| Open (verify + decode + validate) | 1.70 ms | 587 |

On this machine a peer can verify a full event day (640 records) in 1.1 s. A 2016-class Chromebook or a low-end Android tablet should be assumed several times slower — see the open items below, because that factor has not been measured on real hardware.

## 5. Drift detector operating point

Reproduce with `npm run measure:cusum`. 20,000 trials per cell, fixed-seed PRNG.
48 paired observations per scout — one two-day event — with the drift beginning
halfway through. "Detect" counts only alarms that fire at or after the drift
starts; an earlier one is a false accusation, not a detection.

| k | h | false alarm | detect 1.0σ | detect 1.5σ | detect 2.0σ |
|---:|---:|---:|---:|---:|---:|
| 0.50 | 4 | 23.3% | 88.1% in 5 | 88.8% in 3 | 88.5% in 2 |
| 0.50 | 5 | 8.8% | 94.0% in 7 | 96.3% in 4 | 96.4% in 3 |
| **0.75** | **5** | **1.0%** | **81.2% in 11** | **99.5% in 5** | **99.6% in 3** |
| 1.00 | 4 | 0.6% | 59.6% in 11 | 98.7% in 6 | 99.7% in 3 |
| 1.00 | 5 | 0.1% | 43.3% in 14 | 97.6% in 8 | 100.0% in 4 |

Chance that **at least one** scout on a clean team is falsely accused, in one event:

| k | h | 4 scouts | 6 scouts | 8 scouts |
|---:|---:|---:|---:|---:|
| 0.50 | 4 | 67.2% | 81.2% | 89.2% |
| 0.50 | 5 | 30.9% | 42.5% | 52.2% |
| **0.75** | **5** | **4.0%** | **5.9%** | **7.8%** |
| 1.00 | 4 | 2.8% | 4.1% | 5.4% |
| 1.00 | 5 | 0.3% | 0.5% | 0.6% |

The design specified the textbook 0.5 / 4 pairing. The second table is why it is
not shipped: it accuses somebody innocent at four events out of five on a
six-scout team, and a drift table that is usually wrong stops being read — which
costs more real drifts than a stricter detector misses. D-28 records the change.

What this does **not** establish: the shift sizes are in units of the pool's own
disagreement spread, and no real scouting data was available to check that a
disengaged scout actually drifts by 1σ rather than 0.4σ. The false-alarm column
is a property of the detector and holds regardless; the detection columns are
conditional on that assumption.

## 6. Not measured here — these cells stay empty on purpose

| Open question | Why this harness cannot answer it | Blocks |
|---|---|---|
| Real BLE sustained throughput, GATT peripheral role | Needs two physical devices and a radio; the 12 / 25 kB/s figures above are the design's assumptions, carried through, not measurements | NFR-3, NFR-4 |
| iOS `updateValue` backpressure | Requires an iOS peripheral under load; the failure mode (second and later notifications returning false, ready-callback stalling 50-100 ms) is invisible in a simulator | iOS throughput, NFR-3 |
| Connection setup time, device to device | Radio-dependent; the ~1.8 s used in the LEAF_THRESHOLD derivation is an assumption and the whole threshold rests on it | LEAF_THRESHOLD, D-11 |
| Battery cost of a full event day of gossip | Needs a real device and a real radio duty cycle; D-21 explicitly leaves this to be re-derived after D-11 changed the schedule | NFR-1, D-21 |
| ChromeOS BLE advertising capability | Platform capability question, not a protocol one — and it decides whether a rookie team with only Chromebooks can form a mesh at all | The rookie persona |
| Outbound queue depth for the Capacitor transport | `MAX_PENDING_WRITES` is 32, chosen so a bound EXISTS rather than measured. The right depth is one connection interval's worth of packets, which needs two real devices | Send-side throughput on device |
| Verify throughput on 2016-class hardware | This machine is not the target device; the ratio to a Celeron N3060 Chromebook is unknown | NFR-6, catch-up time |

