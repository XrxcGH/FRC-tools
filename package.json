{
  "name": "frc-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Courier, Season Pack, and Ledger — infrastructure beneath the FRC scouting-app rebuild",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test --test-timeout 60000 \"packages/*/test/*.test.ts\"",
    "measure": "node --experimental-strip-types packages/courier-core/bench/measure.ts",
    "vectors": "node --experimental-strip-types spec/generate-vectors.ts",
    "demo": "node --experimental-strip-types demo/event-day.ts"
  }
}
